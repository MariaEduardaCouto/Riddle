<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.riddle">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Riddle">
        <activity
            android:name=".scenes.create.CreatePostActivity"
            android:exported="false" />
        <activity
            android:name=".scenes.main.MainActivity"
            android:exported="false" />
        <activity
            android:name=".scenes.login.LoginActivity"
            android:exported="false"
            android:noHistory="true" />
        <activity
            android:name=".scenes.feed.FeedActivity"
            android:exported="false" />
        <activity
            android:name=".scenes.register.Register"
            android:exported="false"
            android:noHistory="true" />
        <activity
            android:name=".scenes.boot.BootActivity"
            android:exported="true"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>