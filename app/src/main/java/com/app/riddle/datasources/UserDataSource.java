package com.app.riddle.datasources;

public class UserDataSource {

    void createUser(){
        /*
        * Map<String, Object> map = new HashMap<>();
                map.put(  "email", email);
                map.put(  "phone", phone);
                map.put(  "password", password);

                String id = auth.getCurrentUser().getUid();

                dataBase.child("Users").child(id).setValue(map).addOnCompleteListener(new OnCompleteListener<Void>() {
                    @Override
                    public void onComplete(@NonNull Task<Void> task2) {
                        if (task2.isSuccessful()){
                            startActivity(new Intent(Register.this, FeedActivity.class));
                            finish();
                        }else{
                            Toast.makeText(Register.this, "It´s not possible to create an account with the data provided.", Toast.LENGTH_SHORT).show();
                        }

                    }
                });*/
    }
}
